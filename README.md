# Shorthack

ИИ-помощник поддержки МИСИС: разбирает обращение, находит ответ в базе знаний,
а если ответа нет — собирает обращение и отправляет его в поддержку.

Языковая модель — **Groq** (OpenAI-совместимый API). Развёртывание — свой контейнер на нашем VPS.

## Быстрый старт

```bash
cp .env.example .env   # вписать GROQ_API_KEY
```

Секреты в репозиторий не коммитим: `.env` в `.gitignore`, в PR попадает только `.env.example`.
Ключ не пересылаем в чатах — только файлом на машине.

## Команда

| Кто | Роль | Зона ответственности |
|---|---|---|
| alexa | бэкенд, алгоритмы, архитектура | ядро, вызовы модели, контейнер и VPS |
| nyonless | бэкенд | изолированные модули по фиксированным контрактам |
| antitaxi | дизайн | макеты, фронт-прототип, слайды, демо |
| Стася | данные | база знаний и тестовые обращения |

## Идея

**ИИ-помощник поддержки МИСИС** — отдельный сайт, куда обучающийся или поступающий пишет
вопрос своими словами: про поступление, обучение или внеучебную деятельность. Помощник разбирает обращение и
делает одно из двух:

1. **Отвечает сам** — находит подходящий раздел на `misis.ru`, даёт короткую выжимку по сути
   вопроса и ссылку на первоисточник. Вопрос закрыт за секунды, заявка не создаётся.
2. **Предлагает создать заявку** — если ответа в базе нет, вопрос нетиповой или требует
   разбора человеком. Помощник заранее собирает всё, что нужно оператору, и дописывает
   недостающее уточняющими вопросами.

Роль — **поступающий, студент или преподаватель** — человек выбирает сам ползунком на
главном экране. Она задаёт, в какой части базы знаний искать и куда уйдёт обращение, поэтому
модели не приходится её угадывать.

Обращение можно отправить двумя способами. **С подтверждением почты** — человек вводит любой
адрес (у поступающих корпоративного нет), получает код, подтверждает владение ящиком и
получает ответ письмом. **Инкогнито** — без подтверждения: обращение дойдёт до поддержки и
попадёт в статистику, но ответить на него будет некуда, и об этом честно предупреждаем до
отправки.

Поступающие — половина аудитории и самая уязвимая её часть: у них ещё нет ни корпоративной
почты, ни личного кабинета, а вопросы самые срочные. Требовать от них корпоративный адрес
было бы отсечением половины пользователей.

### Зачем это нужно

Сейчас человек либо ищет ответ по сайту вручную, либо пишет письмо «в никуда» и ждёт.
Для поступающего это особенно болезненно: сроки подачи документов жёсткие, а сайт большой.
Поддержка в ответ получает обращения без половины нужных данных и тратит время на переписку
ради уточнений.

Помощник снимает обе боли сразу: типовые вопросы закрываются ссылкой на существующий раздел
и не доходят до людей, а то, что доходит, приходит уже структурированным, с подтверждённым
автором и без круга уточнений.

### Поток

```
главный экран: ползунок «поступающий / студент / преподаватель» + «Задать вопрос»
   │
   ├── РАЗДЕЛ «ЗАДАТЬ ВОПРОС» — привычный чат языковой модели
   │      ├─[код]   роль уже известна → берём индекс FAQ только её аудитории
   │      ├─[LLM]   разбор вопроса: суть, сколько вопросов задано, ключевые понятия
   │      ├─[алг]   поиск по индексу ключевых слов (в промпте только они, без содержания)
   │      ├─[инстр] get_article(id) — подгружает текст только выбранных разделов
   │      ├─[инстр] fetch_page(url) — если в базе нет, читает раздел misis.ru
   │      ├─ нашли ──→ выжимка по вопросу + ссылка на первоисточник
   │      └─ не нашли ─→ «Ответа у меня нет. Создать обращение?»
   │
   └── РАЗДЕЛ «СОЗДАТЬ ОБРАЩЕНИЕ»
          ├─ развилка: подтвердить почту кодом ИЛИ остаться инкогнито
          ├─[код]   проверка полноты по реестру полей роли и категории
          ├─[LLM]   уточняющие вопросы строго по недостающим полям
          ├─[LLM]   сборка текста обращения и темы письма
          └─[код]   письмо в поддержку; ответ приходит только авторизованным
```

### Почему поиск двухступенчатый

В промпт кладутся **только ключевые слова и заголовки** разделов нужной аудитории — не их
содержание. Модель выбирает, что именно открыть, и содержание подгружается инструментом по
идентификатору. Если раздела нет и в базе — инструмент читает страницу misis.ru напрямую.

Это даёт три вещи разом: запрос не раздувается и остаётся в лимитах, база знаний растёт без
переписывания промпта, а сам переход «выбрал раздел → открыл его → ответил» и есть агентная
часть проекта, которую требует кейс.

### Экраны

1. **Главный** — ползунок роли (поступающий / студент / преподаватель) и кнопка «Задать вопрос».
2. **Чат помощника** — привычный интерфейс языковой модели, без лишних настроек. Два раздела:
   «Задать вопрос» и «Создать обращение».
3. **Предложение обращения** — появляется прямо в чате, когда ответа в базе нет.
4. **Развилка доступа** — «подтвердить почту» или «остаться инкогнито» с честным
   предупреждением: без подтверждения ответ прийти не сможет.
5. **Подтверждение почты** — адрес, затем код из письма.
6. **Уточнение** — помощник спрашивает только то, чего не хватает.
7. **Обращение отправлено** — номер, тема письма, что будет дальше.

### Профиль и расписание

После подтверждения почты человек один раз рассказывает о себе: студент — институт и группу,
преподаватель — кафедру и фамилию. Поступающему заполнять нечего. Данные сохраняются за
почтой, при следующем входе не спрашиваются.

Это открывает быстрые кнопки: **«Расписание на сегодня»**, «на завтра», «на выбранный день».
Помощник уже знает группу и отвечает списком пар — время, предмет, тип занятия,
преподаватель, аудитория.

**Расписание разбирается без языковой модели.** Это таблица, а не текст: чистый алгоритм
читает `.xls` по институтам и отдаёт нужный день. Модель здесь только добавила бы задержку,
стоимость и шанс выдумать несуществующую пару. Формат файлов разобран в
[team/nyonless/task/format-raspisaniya.md](team/nyonless/task/format-raspisaniya.md).

Это же — лучший ответ на вопрос жюри «где вы применяете модель, а где обычную логику»:
у нас есть живой пример задачи, от которой модель осознанно отключена.

### Хранение данных

Все данные лежат в одном томе: база знаний, файлы расписаний, SQLite с профилями и
обращениями, кэш ответов модели, исходящие письма. Приложение обращается к нему через
`DATA_DIR` и не знает, запущено оно локально или в контейнере.

## План

| Этап | Что готово | Срок |
|---|---|---|
| 0 | Каркас, контракты API зафиксированы, все получили задачи | T+0:30 |
| 1 | Сквозной путь: вопрос → LLM-разбор → поиск по FAQ → выжимка со ссылкой | T+2:00 |
| 2 | Путь заявки: проверка полноты, уточняющие вопросы, сборка карточки | T+3:00 |
| 3 | Авторизация по коду + отправка письма (в mock-режиме, если SMTP не пустят) | T+3:45 |
| 4 | Интеграция фронта, реальный SMTP, наполнение FAQ | T+4:45 |
| 5 | **Заморозка кода.** Прогон демо, слайды | T+5:00 |
| 6 | Репетиция защиты | T+5:30 |

Обязательный минимум ТЗ закрыт уже на этапе 1. Всё после — наращивание ценности,
и любой этап можно отрезать без потери работоспособности демо.

### Зоны

| Кто | Зона | Задача |
|---|---|---|
| alexa + Sonnet | ядро и инфраструктура | LLM-разбор, поиск по базе знаний, инструменты агента, сборка обращения, API, контейнер и хранилище |
| nyonless | серверная логика без модели | подтверждение почты, профили, отправка писем, разбор расписаний |
| antitaxi | интерфейс | экраны, профиль, кнопки расписания, слайды защиты |
| Стася | данные | база знаний по misis.ru волнами, тестовые обращения |

Постановки лежат в папке своего исполнителя (`team/<кто>/task/`) и заведены задачами на GitHub —
работаем по ним, статус смотрим там же:

| # | Задача | Кто | Когда | Постановка |
|---|---|---|---|---|
| [1](../../issues/1) | Подтверждение почты, профили, письма | nyonless | T+2:30 | [01](team/nyonless/task/01-nyonless-auth-mail.md) |
| [2](../../issues/2) | Интерфейс: экраны, профиль, расписание | antitaxi | T+3:00 | [02](team/antitaxi/task/02-antitaxi-ui.md) |
| [3](../../issues/3) | База знаний волнами и тестовые обращения | Стася | волнами с T+1:00 | [03](team/stasya/task/03-stasya-faq-data.md) |
| [4](../../issues/4) | Ядро агента | alexa | T+4:00 | [04](team/alexa/task/04-core-agent.md) |
| [5](../../issues/5) | Разбор расписания без модели | nyonless | T+4:00 | [05](team/nyonless/task/05-nyonless-schedule-parser.md) |
| [6](../../issues/6) | Контейнер и хранилище | alexa | T+3:30 | [06](team/alexa/task/06-infra.md) |

Постановку можно целиком скопировать в чат с моделью — они написаны так, чтобы
исполнителю не пришлось ничего додумывать.

## Куда что класть

Папки созданы заранее, в каждой лежит `README.md` с правилами. Ничего не создавайте рядом —
пути зашиты в коде и в постановках.

```
team/<кто>/          папка участника: маршрутный лист, постановка, свои материалы
app/                 серверный код (Python)
web/                 интерфейс: index.html, style.css, app.js
tests/               тесты
data/faq/            база знаний по ролям
data/schedule/       .xls расписаний по институтам
docs/                контракты, разбор кейса, донорский проект
```

Начинать всем с [`team/<кто>/README.md`](team/) — там маршрутный лист: что делаешь,
что прочитать, куда класть результат. Постановка лежит там же.

Код и данные, которые читает приложение, остаются на общих путях — иначе оно не запустится.
Всё некодовое — в своей папке, в `materials/`.

## Работа в команде

**Ветки**

- `main` — защищённая, только через PR. Всегда должна собираться.
- Рабочие ветки: `feat/<area>-<кратко>`, `fix/<area>-<кратко>`, например `feat/core-llm-client`.
- Одна задача = одна ветка = один PR. Ветку берём от свежего `main`.

**Правила, чтобы не ловить конфликты**

- За каждым участником закреплена своя зона (каталог/модуль). В чужую зону — только через договорённость.
- Контракты (схемы запросов/ответов, интерфейсы модулей) фиксируются ДО реализации и меняются отдельным PR с уведомлением команды.
- `git pull --rebase origin main` перед пушем.

**PR**

- Название: `[area] что сделано`.
- В описании: что делает, как проверить, какие переменные окружения нужны.
- Мержим только зелёный PR; ревью — минимум один человек.

**Задачи**

Постановка лежит в папке своего исполнителя: `team/<кто>/task/` — самодостаточное ТЗ, которое можно целиком скопировать в чат с LLM и получить рабочий код. Каждой постановке соответствует задача на GitHub, там же указано, в какую папку класть результат.

## Decision log

Правило: **каждое принятое решение немедленно попадает сюда**. Новая запись — в конец таблицы, старые не переписываем. Если решение отменено — не удаляем, а добавляем новую строку со статусом `отменяет #N`.

| # | Решение | Почему | Статус |
|---|---|---|---|
| 1 | Авторизация в Yandex Cloud по API-ключу сервисного аккаунта (`Authorization: Api-Key`) | Без IAM-токенов и их протухания — меньше движущихся частей на 6 часов | принято |
| 2 | Дедлайн 6 часов; code freeze за час до сдачи | Живое демо важнее функционала; час нужен на стабилизацию и репетицию | принято |
| 3 | Стек по умолчанию: Python 3.11 + FastAPI + httpx + pydantic, без БД (in-memory/SQLite) | Максимальная скорость сборки; посильно для участника на бесплатном тарифе LLM | принято |
| 4 | Порядок работы: сначала сквозная заглушка end-to-end, потом наполнение логикой | Гарантирует, что к дедлайну есть что показать, даже если не всё готово | принято |
| 5 | Задачи участникам оформляются как самодостаточные карточки-постановки | У команды бесплатные тарифы LLM без доступа к репозиторию — ТЗ должно быть копипастным | принято |
| 6 | Decision log ведётся в README и обновляется по ходу работы | Четверо работают параллельно, устные договорённости теряются | принято |
| 7 | Зона третьего участника (Стася) — вне критического пути | Участие под вопросом; план не должен рассыпаться при его отсутствии | принято |
| 8 | Донор для переиспользования — `OsadaSupportBot`: берём правила общения (системный промпт), таксономию причин, разбор вложений | Промпт уже выверен в бою: тональность, «строго по базе», дисклеймеры, защита от промпт-инъекций. Писать заново — минус 1,5–2 часа | принято |
| 9 | Провайдер LLM — Yandex Cloud Foundation Models вместо Groq; цепочка фолбэка моделей не переносится | Требование хакатона; у YC другая модель лимитов, фолбэк-движок донора избыточен | принято |
| 10 | Telegram/aiogram-слой, консоль оператора и PostgreSQL из донора НЕ переносим | ~900 строк UI и инфраструктуры, не видны в демо, съедят весь бюджет времени | принято |
| 11 | Хранилище — SQLite (файл в репозитории), схема тикетов упрощена от донорской | Ноль настройки у всех участников, демо заводится из коробки | принято |
| 12 | Режим работы — однопроходный анализ обращения со структурированным JSON-выходом, а не диалоговый чат | По условию помощник *разбирает* обращение и *выполняет действие*; JSON-выход надёжнее tool-calling и сразу рисуется на фронте | принято |
| 13 | Пользовательский ввод всегда трактуется как ДАННЫЕ, не как инструкции модели | Вход — письма и расшифровки от внешних людей; без этого прототип ломается одной строкой в письме | принято |
| 14 | Предметная область демо — поддержка VPN-сервиса, база знаний из донора | Не тратим время на выдумывание домена, ответы сразу выглядят достоверно | предложено |
| 15 | Домен — корпоративная и университетская техподдержка (учётки, образовательные платформы, Wi-Fi, оборудование). **Отменяет #14** | Прямо задан кейсом: СберТех и университетская поддержка. База знаний донора по VPN не подходит, пишем свою | принято |
| 16 | Обработку аудио, реальные интеграции, сложный дашборд и промышленную архитектуру не делаем | Кейс явно называет их необязательными; «расшифровка разговора» приходит уже текстом | принято |
| 17 | Сценарий — «Диспетчер первой линии»: обращение → карточка заявки → выбор и выполнение действия | Кейс требует один законченный сценарий; этот закрывает весь правый столбец ТЗ разом | принято |
| 18 | Одно обращение раскладывается на N самостоятельных заявок | «Несколько вопросов в одном сообщении» — явный пункт входа в ТЗ, который обычно игнорируют | принято |
| 19 | LLM не принимает решений: она извлекает структуру и генерирует текст. Приоритет, маршрут, достаточность данных и выбор действия — детерминированный код | «Где LLM, а где обычная логика» — отдельный пункт защиты; так решения воспроизводимы и покрываются тестами | принято |
| 20 | В каждой заявке ведётся `trace`: что решила модель, что решил код и по какому правилу | Прямой ответ на вопрос защиты «как результат LLM влияет на действия программы» | принято |
| 21 | Ответы LLM кэшируются на диск по хэшу входа | Страховка от лимитов и падения сети организаторов во время демо | принято |
| 22 | Продукт — отдельный сайт помощника поддержки МИСИС; пользователь — студент или сотрудник, а не оператор. **Уточняет #17** | Конкретный пользователь и живой домен читаются жюри лучше абстрактной «первой линии» | принято |
| 23 | Два исхода разбора: ответ выжимкой со ссылкой на раздел misis.ru ИЛИ создание заявки | Закрывает типовые вопросы без участия людей и разгружает поддержку — измеримая польза | принято |
| 24 | Домен вопросов — образование и внеучебная деятельность МИСИС | Есть открытый первоисточник (misis.ru) для базы FAQ, не выдумываем содержание | принято |
| 25 | Заявка создаётся только после подтверждения корпоративной почты кодом | Отсекает анонимный спам и даёт поддержке проверенного автора | принято |
| 26 | Готовая заявка уходит письмом на почту поддержки с единым форматом темы | Реальный канал вместо выдуманной интеграции; тема разбирается фильтрами почты | принято |
| 27 | Отправка почты имеет mock-режим: письма пишутся в файл и показываются в интерфейсе | SMTP на площадке может быть закрыт; демо не должно зависеть от сети | принято |
| 28 | Поиск по FAQ — гибрид: ключевые слова + эмбеддинги, а не только ключевые слова | Чистые ключевые слова ломаются на формулировках «своими словами», что и есть суть кейса | принято |
| 29 | Адрес почты поддержки хранится в переменной окружения, а не в коде | Репозиторий приватный, но личные адреса в git не попадают | принято |
| 30 | Контракты HTTP-API фиксируются до начала кодинга и меняются только уведомлением команды | Четверо работают параллельно и мокают чужие куски по контракту | принято |
| 31 | Аудитория — обучающиеся **и поступающие** МИСИС. **Уточняет #22 и #24** | Мы внутри инфраструктуры поддержки абитуриентов и студентов; у поступающих вопросы срочнее и сайт для них сложнее | принято |
| 32 | Код подтверждения шлётся на **любую** почту; корпоративный домен лишь определяет тип заявителя и адресата заявки. **Отменяет часть #25** | У поступающих нет корпоративной почты — жёсткий белый список отрезал бы половину аудитории | принято |
| 33 | Роль (поступающий / студент / преподаватель) выбирается ползунком на главном экране, а не определяется моделью | Дешевле и точнее: экономит вызов, задаёт фильтр базы знаний и адресата обращения | принято |
| 34 | База FAQ разделена на три части по ролям | В запрос попадает только релевантная аудитории часть — меньше шума и токенов | принято |
| 35 | Двухступенчатый поиск: в промпте только ключевые слова и заголовки, содержание подгружается инструментом по идентификатору | Запрос остаётся в лимитах, база растёт без переписывания промпта | принято |
| 36 | Инструменты агента: `search_faq`, `get_article`, `fetch_page`, `create_ticket` | Это и есть «агентная часть» из кейса: переход от анализа к действию через функции программы | принято |
| 37 | Интерфейс — привычный чат языковой модели с двумя разделами: «Задать вопрос» и «Создать обращение» | Знакомый паттерн, объяснять жюри и пользователю нечего | принято |
| 38 | Обращение можно отправить инкогнито, без подтверждения почты, но без обратной связи; помечается как анонимное | Не заставляем авторизовываться ради разового вопроса; анонимные обращения всё равно ценны как сигнал о массовой проблеме | принято |
| 39 | `fetch_page` ходит только по белому списку доменов (`misis.ru`) | Иначе текст пользователя уводит агента на произвольный сайт — прямая дыра | принято |
| 40 | За подтверждённой почтой сохраняется профиль: студент — институт и группа, преподаватель — кафедра и фамилия; поступающему профиль не нужен | Человек заполняет это один раз, дальше помощник сам знает, чьё расписание показывать | принято |
| 41 | Авторизованным доступны быстрые кнопки расписания: сегодня, завтра, выбранный день | Самый частый запрос студента, закрывается одним нажатием без диалога с моделью | принято |
| 42 | Расписание разбирается и выдаётся **чистым алгоритмом, без языковой модели** | Таблица не требует понимания текста; модель добавила бы задержку, стоимость и риск выдуманных пар. Это наш пример «где модель не нужна» для защиты | принято |
| 43 | Источник расписания — файлы `.xls` по институтам, формат разобран заранее | Реверс-инжиниринг формата отдан не исполнителю, а сделан один раз в `team/nyonless/task/format-raspisaniya.md` | принято |
| 44 | Чётность учебной недели считается от `SEMESTER_START` из окружения | В файлах нет календарных дат, только дни недели и деление на верхнюю/нижнюю | принято |
| 45 | Все данные лежат в одном томе, путь приходит из `DATA_DIR` | Одинаково работает локальный запуск и контейнер, данные переживают пересборку | принято |
| 46 | Контейнер обязателен, вынос на VPS — только после заморозки кода | Кейс прямо говорит, что достаточно локального прототипа; внешний сервер во время работы — потерянный час и лишняя точка отказа на защите | принято |
| 47 | Сбор базы знаний идёт волнами: сначала вширь по всем темам, потом вглубь | Данных на сайте больше, чем можно собрать за хакатон; после любой волны база остаётся рабочей | принято |
| 48 | Каждая постановка заведена задачей на GitHub, с меткой направления и приоритетом | Один список на всю команду вместо переписки: видно, кто чем занят и что блокирует остальных | принято |
| 49 | Провайдер модели — **Groq**, свой ключ. **Отменяет #1 и #9** (Yandex Cloud) | Ключи организаторов не нужны, свой ключ уже есть и работает; API OpenAI-совместимый, клиент проще | принято |
| 50 | Модель по умолчанию `openai/gpt-oss-120b`, запасная `qwen/qwen3.8-27b` | Обе проверены на живом разговорном обращении: категория и оба вопроса извлекаются верно. Запасная втрое дешевле по токенам и не тратит reasoning | принято |
| 51 | Возвращаем цепочку фолбэка моделей из донора (частично отменяет #9) | У Groq лимит 8000 токенов в минуту на всю команду: на 429 переключаемся на запасную модель, а не падаем | принято |
| 52 | Поиск по базе знаний остаётся по ключевым словам, эмбеддинги не используем | У Groq нет эмбеддингов, а двухступенчатый поиск (#35) в них и не нуждается — одной зависимостью меньше | принято |
| 53 | Дисковый кэш ответов модели обязателен, перед защитой прогревается на сценарии демо | 8000 токенов в минуту делятся на четверых; повторный вопрос на демо не должен упираться в лимит | принято |
| 54 | Разворачиваем на своём VPS в своём контейнере. **Отменяет #46** | VPS и доступы уже есть; публичный адрес на защите выглядит сильнее локального прототипа | принято |
| 55 | У каждого участника своя папка `team/<кто>/`: маршрутный лист, постановка, `materials/` под некодовое | Человек открывает одну папку и видит всё своё; макеты и черновики не лежат вперемешку с кодом | принято |
| 56 | Постановки переехали из `docs/tasks/` в папку своего исполнителя | Задача должна лежать там, где человек работает, а не в общей куче документов | принято |
| 57 | База знаний живёт в `data/faq/`, а не россыпью в `data/` | Совпадает с раскладкой тома в контейнере, не нужно переучивать пути при деплое | принято |

## Структура

Будет зафиксирована вместе с условием задачи.
