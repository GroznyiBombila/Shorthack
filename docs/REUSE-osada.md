# Карта переиспользования: OsadaSupportBot → Shorthack

Донор: `C:\Users\alexa\VSCode\OsadaSupportBot` (Telegram-бот поддержки OSADA VPN на Groq).
Берём отлаженное, выкидываем всё, что не видно в демо.

## Берём (порт с адаптацией)

| Из донора | Что это | Куда у нас | Зачем |
|---|---|---|---|
| `groq_agent.BASE_SYSTEM_PROMPT` | Правила общения: тон, «строго по базе знаний», что не делает, дисклеймеры, антипромпт-инъекция, стиль | `app/prompts.py` | **Основная просьба.** Готовый выверенный промпт — экономит часы подбора формулировок |
| блок «БЕЗОПАСНОСТЬ» из промпта | Текст пользователя = ДАННЫЕ, а не инструкции | `app/prompts.py` | По условию на вход идут письма клиентов — защита от инъекций обязательна |
| `groq_agent.TOOLS` (схемы) | Декларация действий с `reason`/`mode`/`summary` | `app/schemas.py` | Готовая таксономия причин и режимов → наши «категория» и «следующий шаг» |
| `escalation.do_escalate` (логика) | Создание заявки, карточка, резюме для оператора | `app/actions.py` | Это и есть «выполняет действие: создаёт заявку» |
| `escalation.REASON_RU`, `_card_text` | Человекочитаемые категории и рендер карточки обращения | `app/render.py` | Готовый формат вывода для демо |
| `documents.read_document` | PDF/DOCX/TXT → текст, с обрезкой | `app/ingest/documents.py` | Вход «письмо пользователя» файлом |
| `audio.transcribe_audio` | Транскрипция голоса | `app/ingest/audio.py` | Вход «текстовая расшифровка разговора» (Groq → YC SpeechKit) |
| `vision.analyze_image` | Разбор скриншота | `app/ingest/vision.py` | Опционально, если останется время |
| `groq_agent._truncate`, `_est_tokens`, бюджет запроса | Защита от переполнения контекста | `app/llm_client.py` | Провайдер тот же, переносится как есть |
| `db` схема тикетов (`create_ticket`, статусы) | Модель заявки | `app/storage.py` | Упрощаем PostgreSQL → SQLite |
| `knowledge_base.md` | База знаний по VPN-поддержке | `data/knowledge_base.md` | Готовый домен для демо — не выдумываем предметную область |

## Не берём

| Что | Почему |
|---|---|
| `bot.py`, `ui.py`, aiogram-слой, баннеры | Канал демо — веб, не Telegram |
| Консоль оператора (`escalation.py`, ~900 строк UI) | Не влезет в 6 часов и не нужно для оценки |
| Операторская консоль, подписки, PostgreSQL | Не нужны на прототип |
| PostgreSQL/asyncpg, пул, ретеншн | SQLite достаточно для прототипа |
| Подписки, `get_subscription_status`, бан-лист | Вне условия задачи |

## Что меняется принципиально

- **Провайдер**: остаётся Groq. Вызовы переписываем с SDK на прямой HTTP (`Authorization: Bearer`), модели другие: `openai/gpt-oss-120b` и запасная `qwen/qwen3.8-27b`. Цепочка фолбэка и разбор 429/413 переносятся как есть — при лимите 8000 токенов в минуту на команду они нужны.
- **Режим работы**: донор — диалоговый Q&A-бот; у нас — **однопроходный анализатор обращения**, отдающий структуру (JSON), а не реплику.
- **Выход**: вместо текста ответа — структурированная карточка: суть, категория, срочность, недостающие данные, список вопросов из обращения, следующий шаг и готовое действие.
